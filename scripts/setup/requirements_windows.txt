# VoiceFlow 3.0 Windows Requirements
# Core dependencies for VoiceFlow voice transcription system

# Audio processing
sounddevice>=0.4.6
numpy>=1.24.0

# Speech recognition - VoiceFlow 3.0 unified ASR engine
# Supports: Distil-Whisper (6x faster), standard Whisper, Voxtral
faster-whisper>=1.0.0
torch>=2.0.0

# For Voxtral model support (optional)
transformers>=4.40.0
accelerate>=0.30.0

# Input handling
keyboard>=0.13.5
pyautogui>=0.9.54

# UI and system tray
Pillow>=10.0.0
pystray>=0.19.4

# Configuration and utilities
python-dotenv>=1.0.0
psutil>=5.9.0

# Optional: GPU acceleration (CUDA)
# torch[cuda] # Uncomment if using CUDA

# Optional: WhisperX for advanced features (diarization, word timestamps)
# whisperx

# Development and testing
pytest>=7.0.0
pytest-asyncio>=0.21.0