# VoiceFlow Personal - Minimal Requirements
# Optimized for speed, privacy, and efficiency

# Core transcription (required)
RealtimeSTT>=1.3.0

# System integration (required for text injection)
pyautogui>=0.9.50
keyboard>=0.13.5

# HTTP client for AI enhancement (optional but recommended)
requests>=2.31.0

# GPU acceleration (optional - install if you have NVIDIA GPU)
# torch>=2.0.0
# torchaudio>=2.0.0

# Note: Install GPU packages separately if available:
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu118